#include<bits/stdc++.h>
using namespace std;
map <string,vector<string> > m;
map <string,vector<String> >::iterator f,smi;
int main(){
	string input;
	while(1){
		cin>>input;
		if(input[0]=='-')	break;
		string key=input.substr(0,input.find('-'));
		//setting that to 1 means it sused
		NT[key[0]-'A']=1;
		int next,prev=
		
	}

}

